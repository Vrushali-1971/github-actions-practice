name: Run Real Tests in CI

on: 
  workflow_dispatch:

jobs:
  tests:
    runs-on: ubuntu-latest
    
    steps: 
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Make script executable
        run: chmod +x log_summary.sh

      - name: Run script
        run: ./log_summary.sh
